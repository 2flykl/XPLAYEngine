import { apiUrl } from '../core/ApiBase.js';
export async function analyzeVisualSource(imageDataUrl,prompt=''){const endpoint=apiUrl('/api/vision/analyze');if(!endpoint)return{ok:false,provider:'offline',error:'Semantic AI Vision unavailable.',analysis:{player:'Semantic AI Vision unavailable',environment:'Semantic AI Vision unavailable',vehicles:'Semantic AI Vision unavailable',notableObjects:'Semantic AI Vision unavailable',dominantColors:'',strongOpportunities:'Genre not semantically confirmed',qualityScore:0,qualityLabel:'offline'}};const r=await fetch(endpoint,{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify({imageDataUrl,prompt})});const out=await r.json().catch(()=>({}));if(!r.ok||!out?.ok)return{ok:false,provider:out?.provider||'offline',error:out?.error||`Vision HTTP ${r.status}`,analysis:{player:'Semantic AI Vision unavailable',environment:'Semantic AI Vision unavailable',vehicles:'Semantic AI Vision unavailable',notableObjects:'Semantic AI Vision unavailable',dominantColors:'',strongOpportunities:'Genre not semantically confirmed',qualityScore:0,qualityLabel:'offline'}};return out;}
export async function visionHealth(){const endpoint=apiUrl('/api/vision/health');if(!endpoint)return{ok:false,provider:'offline',configured:false,error:'No live XPLAY API base configured.'};try{const r=await fetch(endpoint,{cache:'no-store'});return await r.json();}catch(e){return{ok:false,provider:'offline',configured:false,error:e.message}}}
